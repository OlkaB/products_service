export class Product {
    constructor(
        public name: string,
        public img: string,
        public criteria: object,
        public price: number,
        public description: string
    ) { }
}
